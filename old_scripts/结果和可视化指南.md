# 📊 实验结果和可视化完整指南

---

## 📁 结果存储位置

### 结构总览

```
results/
├── structural_fingerprint/          ← main_experiment.py 的结果
│   ├── cora_20251229_164530_results.json      ← JSON格式（完整数据）
│   ├── cora_20251229_164530_report.txt        ← TXT格式（可读报告）
│   ├── facebook_20251229_173434_results.json
│   └── facebook_20251229_173434_report.txt
│
├── unified/                         ← main_experiment_unified.py 的结果
│   ├── facebook_ego0_20251229_*.json
│   └── cora_20251229_*.json
│
└── figures/                         ← 所有可视化图表
    ├── fig1_identity_deanonymization.png      ← 身份去匿名化对比
    ├── fig2_attribute_inference.png           ← 属性推断对比
    ├── fig3_robustness_curve.png              ← 鲁棒性曲线
    ├── fig4_privacy_utility_tradeoff.png      ← 隐私-效用权衡
    ├── fig5_comprehensive_comparison.png      ← 综合对比
    ├── dashboard.html                         ← 交互式仪表板
    ├── cora_dashboard.html                    ← Cora专用仪表板
    ├── facebook_ego0_experiment_report.html   ← Ego网络报告
    └── summary_report.txt                     ← 文本总结报告
```

---

## 🎯 不同脚本的结果存储位置

### 1. `main_experiment_unified.py` （推荐使用）

**运行命令：**
```bash
python main_experiment_unified.py \
    --dataset facebook_ego \
    --ego_id 0 \
    --mode attack \
    --save         ← 必须加--save才会保存
```

**结果位置：**
```
results/unified/facebook_ego0_20251229_HHMMSS.json
```

**结果格式：**
```json
{
  "dataset": "facebook_ego",
  "ego_id": "0",
  "timestamp": "2025-12-29T20:00:00",
  "graph_stats": {
    "nodes": 333,
    "edges": 2519,
    "avg_degree": 15.13,
    ...
  },
  "deanonymization": [
    {
      "level": "温和",
      "method": "Baseline-Greedy",
      "accuracy": 0.3664,
      "precision@5": 0.5826,
      "mrr": 0.4521
    },
    {
      "level": "温和",
      "method": "Node-Features",
      "accuracy": 0.7057,
      "precision@5": 0.8132,
      "mrr": 0.7518
    },
    ...
  ],
  "attribute_inference": [...],
  "robustness": [...],
  "defense": [...]
}
```

---

### 2. `main_experiment.py` （已归档）

**结果位置：**
```
results/structural_fingerprint/
├── cora_20251229_164530_results.json    ← JSON完整数据
└── cora_20251229_164530_report.txt      ← TXT可读报告
```

**结果格式（JSON）：**
```json
{
  "dataset": "cora",
  "timestamp": "...",
  "stage1_identity": {
    "Baseline": {...},
    "DeepWalk": {...}
  },
  "stage1_attribute": {
    "RandomForest": {...},
    "LabelPropagation": {...}
  },
  "stage2_robustness": {...},
  "stage3_defense": {...}
}
```

**结果格式（TXT报告）：**
```
======================================================================
Experiment Results Summary Report
Generated: 2025-12-29 16:04:48
======================================================================

【Stage 1】Identity De-anonymization Attack
----------------------------------------------------------------------

Baseline:
  - Accuracy: 1.55%
  - Improvement Factor: 42.0x
  - Random Baseline: 0.0369%

【Stage 1】Attribute Inference Attack
----------------------------------------------------------------------

LabelPropagation:
  - Accuracy: 82.75%
  - F1 (macro): 0.8083

...
```

---

## 🎨 可视化（绘图）代码

### 📋 可视化脚本总览

| 脚本 | 输入 | 输出 | 推荐度 |
|------|------|------|--------|
| **`visualize_html.py`** | 任意JSON | 交互式HTML | ⭐⭐⭐⭐⭐ 最推荐 |
| **`visualize_results.py`** | structural_fingerprint的JSON | 5张PNG图 | ⭐⭐⭐⭐ 适合论文 |
| `visualize_unified_results.py` | unified的JSON | PNG图 | ⭐⭐⭐ 专用 |
| `visualize_ego0_html.py` | ego网络数据 | HTML | ⭐⭐⭐ 专用 |

---

### 🌟 方案1：交互式HTML仪表板（最推荐）

#### 脚本：`visualize_html.py`

**特点：**
- ✅ 交互式图表（悬停查看数据）
- ✅ 美观现代（使用Chart.js）
- ✅ 跨平台兼容（无字体问题）
- ✅ 支持任意JSON格式

**使用方法：**

```bash
# 1. 可视化unified脚本的结果
python visualize_html.py results/unified/facebook_ego0_*.json

# 2. 可视化structural_fingerprint的结果
python visualize_html.py results/structural_fingerprint/cora_*.json

# 3. 查看生成的HTML
open results/figures/dashboard.html
```

**生成的文件：**
- `results/figures/dashboard.html` - 交互式仪表板
- `results/figures/{dataset}_dashboard.html` - 数据集专用版本

**HTML内容包含：**
1. 📊 身份去匿名化对比图（柱状图）
2. 📊 属性推断对比图（柱状图）
3. 📈 鲁棒性曲线图（折线图）
4. 📈 隐私-效用权衡图（散点图）
5. 📊 综合对比图（雷达图）

**示例截图：**
```
┌─────────────────────────────────────────┐
│  身份去匿名化攻击结果对比                 │
├─────────────────────────────────────────┤
│  ████████░░░░  Baseline-Greedy (36.64%) │
│  ███░░░░░░░░  Hungarian (16.52%)        │
│  ███████████  Node-Features (70.57%) ⭐ │
│  ██████████░  DeepWalk (待测试)         │
└─────────────────────────────────────────┘
```

---

### 📄 方案2：静态PNG图表（适合论文）

#### 脚本：`visualize_results.py`

**特点：**
- ✅ 高分辨率PNG（300 DPI）
- ✅ 适合论文发表
- ✅ 可离线查看
- ✅ 生成5张独立图表

**使用方法：**

```bash
# 1. 可视化实验结果
python visualize_results.py results/structural_fingerprint/cora_20251229_164530_results.json

# 2. 查看生成的图表
ls results/figures/fig*.png
open results/figures/fig1_identity_deanonymization.png
```

**生成的文件：**

1. **`fig1_identity_deanonymization.png`** - 身份去匿名化
   - 对比不同方法的准确率、Precision@K、MRR
   
2. **`fig2_attribute_inference.png`** - 属性推断
   - 对比不同方法的准确率、F1-score
   
3. **`fig3_robustness_curve.png`** - 鲁棒性曲线
   - X轴：图完整度（50%-100%）
   - Y轴：攻击准确率
   - 显示临界点
   
4. **`fig4_privacy_utility_tradeoff.png`** - 隐私-效用权衡
   - X轴：隐私预算ε
   - Y轴：隐私增益 vs 效用保持
   - 显示最佳平衡点
   
5. **`fig5_comprehensive_comparison.png`** - 综合对比
   - 雷达图展示所有指标
   - 对比不同方法的全面性能

**额外生成：**
- `results/figures/summary_report.txt` - 文本总结报告

---

### 🎯 方案3：Unified专用可视化

#### 脚本：`visualize_unified_results.py`

**特点：**
- ✅ 专为unified脚本的JSON格式设计
- ✅ 支持多种匿名化强度对比
- ✅ 自动处理不同数据集

**使用方法：**

```bash
# 可视化unified结果
python visualize_unified_results.py results/unified/facebook_ego0_*.json

# 生成PNG图表
ls results/figures/unified_*.png
```

---

### 🏆 方案4：Ego网络专用HTML

#### 脚本：`visualize_ego0_html.py`

**特点：**
- ✅ 专为Facebook Ego网络设计
- ✅ 展示社交圈分布
- ✅ 展示节点特征统计

**使用方法：**

```bash
python visualize_ego0_html.py

# 生成
open results/figures/facebook_ego0_experiment_report.html
```

---

## 🚀 完整工作流程

### 工作流1：快速实验 + 交互式可视化（20分钟）

```bash
# 步骤1：运行实验（15分钟）
python main_experiment_unified.py \
    --dataset facebook_ego \
    --ego_id 0 \
    --mode attack \
    --save

# 步骤2：生成交互式HTML（1分钟）
python visualize_html.py results/unified/facebook_ego0_*.json

# 步骤3：查看结果（4分钟）
open results/unified/facebook_ego0_*.json      # 查看原始数据
open results/figures/dashboard.html            # 查看可视化
```

**得到：**
- ✅ JSON结果文件（可编程分析）
- ✅ 交互式HTML仪表板（可演示）

---

### 工作流2：完整实验 + 论文图表（60分钟）

```bash
# 步骤1：运行完整实验（50分钟）
python main_experiment.py \
    --dataset cora \
    --mode all

# 步骤2：生成PNG图表（5分钟）
python visualize_results.py \
    results/structural_fingerprint/cora_20251229_164530_results.json

# 步骤3：查看所有图表（5分钟）
open results/figures/fig1_identity_deanonymization.png
open results/figures/fig2_attribute_inference.png
open results/figures/fig3_robustness_curve.png
open results/figures/fig4_privacy_utility_tradeoff.png
open results/figures/fig5_comprehensive_comparison.png
```

**得到：**
- ✅ 5张高清PNG图（300 DPI，适合论文）
- ✅ 文本总结报告

---

### 工作流3：多数据集对比（90分钟）

```bash
# 步骤1：运行多个实验
python main_experiment_unified.py --dataset facebook_ego --ego_id 0 --mode attack --save
python main_experiment_unified.py --dataset cora --mode attack --save
python main_experiment_unified.py --dataset facebook --mode attack --save

# 步骤2：分别可视化
python visualize_html.py results/unified/facebook_ego0_*.json
python visualize_html.py results/unified/cora_*.json
python visualize_html.py results/unified/facebook_*.json

# 步骤3：对比查看
open results/figures/facebook_ego0_dashboard.html
open results/figures/cora_dashboard.html
open results/figures/facebook_dashboard.html
```

**得到：**
- ✅ 3个数据集的对比可视化
- ✅ 可以发现不同数据集的特点

---

## 📊 查看现有结果

### 查看JSON结果

```bash
# 方法1：使用less查看
less results/unified/facebook_ego0_*.json

# 方法2：使用jq美化查看（如果安装了jq）
cat results/unified/facebook_ego0_*.json | jq '.'

# 方法3：使用Python查看
python -c "
import json
with open('results/unified/facebook_ego0_20251229_120000.json') as f:
    data = json.load(f)
    print('数据集:', data['dataset'])
    print('节点数:', data['graph_stats']['nodes'])
    print('去匿名化结果:', len(data.get('deanonymization', [])), '个方法')
"
```

### 查看TXT报告

```bash
# 直接查看
cat results/structural_fingerprint/cora_20251229_164530_report.txt

# 或用less分页查看
less results/structural_fingerprint/cora_20251229_164530_report.txt
```

### 查看PNG图表

```bash
# macOS
open results/figures/fig1_identity_deanonymization.png

# Linux
xdg-open results/figures/fig1_identity_deanonymization.png

# 或用文件管理器打开results/figures/目录
```

### 查看HTML仪表板

```bash
# macOS
open results/figures/dashboard.html

# Linux
xdg-open results/figures/dashboard.html

# 或直接在浏览器中打开文件
```

---

## 🎨 自定义可视化

### 修改图表样式

**编辑 `visualize_html.py`：**

```python
# 修改配色方案（第100行左右）
colors = [
    'rgba(255, 99, 132, 0.6)',   # 红色 → 改成你喜欢的颜色
    'rgba(54, 162, 235, 0.6)',   # 蓝色
    'rgba(255, 206, 86, 0.6)',   # 黄色
    'rgba(75, 192, 192, 0.6)',   # 绿色
]

# 修改图表大小（第50行左右）
canvas_style = "width: 800px; height: 400px;"  # 改大或改小
```

**编辑 `visualize_results.py`：**

```python
# 修改DPI（第30行左右）
plt.savefig('fig1.png', dpi=300, bbox_inches='tight')  # 300改成600更清晰

# 修改图表大小（第40行左右）
fig, ax = plt.subplots(figsize=(12, 6))  # (宽, 高) 单位：英寸
```

---

## 📦 结果文件说明

### JSON文件字段说明

```json
{
  // 基本信息
  "dataset": "facebook_ego",           // 数据集名称
  "ego_id": "0",                       // Ego网络ID（如果适用）
  "timestamp": "2025-12-29T20:00:00",  // 实验时间
  
  // 图统计
  "graph_stats": {
    "nodes": 333,                      // 节点数
    "edges": 2519,                     // 边数
    "avg_degree": 15.13,               // 平均度
    "density": 0.0456,                 // 密度
    "has_labels": true,                // 是否有标签
    "has_features": true               // 是否有特征
  },
  
  // 阶段1：去匿名化结果
  "deanonymization": [
    {
      "level": "温和",                 // 匿名化强度
      "method": "Baseline-Greedy",     // 方法名
      "accuracy": 0.3664,              // Top-1准确率
      "precision@5": 0.5826,           // Precision@5
      "precision@10": 0.7123,          // Precision@10
      "mrr": 0.4521                    // Mean Reciprocal Rank
    },
    ...
  ],
  
  // 阶段2：属性推断结果
  "attribute_inference": [
    {
      "hide_ratio": 0.3,               // 隐藏比例
      "method": "Label-Propagation",   // 方法名
      "accuracy": 0.6145,              // 准确率
      "correct": 51,                   // 正确数
      "total": 83                      // 总数
    },
    ...
  ],
  
  // 阶段3：鲁棒性结果
  "robustness": [
    {
      "missing_ratio": 0.1,            // 缺失率
      "accuracy": 0.1802               // 准确率
    },
    ...
  ],
  
  // 阶段4：防御结果
  "defense": [
    {
      "epsilon": 2.0,                  // 隐私预算
      "edge_preservation": 0.9865,     // 边保留率
      "utility_score": 0.9861          // 效用得分
    },
    ...
  ]
}
```

---

## 🔧 常见问题

### Q1: 运行实验后没有生成结果文件？

**A:** 确保加了 `--save` 参数

```bash
# ❌ 错误：没有--save，只在终端显示
python main_experiment_unified.py --dataset cora --mode attack

# ✅ 正确：有--save，会保存JSON文件
python main_experiment_unified.py --dataset cora --mode attack --save
```

---

### Q2: 可视化脚本找不到JSON文件？

**A:** 使用通配符或完整路径

```bash
# 方法1：使用通配符（推荐）
python visualize_html.py results/unified/facebook_ego0_*.json

# 方法2：使用完整路径
python visualize_html.py results/unified/facebook_ego0_20251229_120000.json

# 方法3：使用tab自动补全
python visualize_html.py results/unified/facebook_ego0_[按Tab键]
```

---

### Q3: 生成的PNG图表中文乱码？

**A:** 这是字体问题，使用HTML版本（无字体问题）

```bash
# 推荐：使用HTML版本
python visualize_html.py results/unified/facebook_ego0_*.json
open results/figures/dashboard.html

# 如果一定要PNG，需要安装中文字体
# macOS: 已经有中文字体，应该不会乱码
# Linux: sudo apt-get install fonts-wqy-zenhei
```

---

### Q4: 想要高分辨率的图表怎么办？

**A:** 修改DPI参数

```python
# 编辑 visualize_results.py
# 找到 plt.savefig(...) 这一行

# 原来
plt.savefig('fig1.png', dpi=300, bbox_inches='tight')

# 改成
plt.savefig('fig1.png', dpi=600, bbox_inches='tight')  # 更高清
```

---

## 📚 推荐使用方案

### 💼 答辩演示

**推荐：** 交互式HTML仪表板

```bash
python main_experiment_unified.py --dataset facebook_ego --ego_id 0 --mode attack --save
python visualize_html.py results/unified/facebook_ego0_*.json
open results/figures/dashboard.html
```

**优点：**
- ✅ 可以悬停查看具体数值
- ✅ 美观现代
- ✅ 可以用浏览器全屏演示

---

### 📄 论文写作

**推荐：** 静态PNG图表

```bash
python main_experiment.py --dataset cora --mode all
python visualize_results.py results/structural_fingerprint/cora_*.json
```

**优点：**
- ✅ 高分辨率（300 DPI）
- ✅ 适合插入Word/LaTeX
- ✅ 可以单独使用每张图

---

### 🔬 数据分析

**推荐：** 直接读取JSON

```python
import json
import pandas as pd

# 读取结果
with open('results/unified/facebook_ego0_20251229_120000.json') as f:
    data = json.load(f)

# 转换为DataFrame
df = pd.DataFrame(data['deanonymization'])

# 分析
print(df.groupby('method')['accuracy'].mean())
```

---

希望这个指南帮到你了！如果还有任何问题，随时问我 😊
